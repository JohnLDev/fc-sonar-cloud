/**
 *
 * @param {Number} n1
 * @param {Number} n2
 * @returns {Number}
 */
const sum = (n1, n2) => {
  const result = n1 + n2;
  console.log({ result });
  return result;
};

module.exports = { sum };
